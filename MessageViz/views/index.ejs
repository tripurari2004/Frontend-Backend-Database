<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MessageViz</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
    <h1 class="text-center pt-3 pb-3">Welcome to MessageViz</h1>
    <form class="form" method="GET" action="/chats/new">
        <button class="btn btn-primary">New Chat</button>
    </form>

    <div class="chats">
        <% for( chat of chats) { %>
            <div class="chat">
                <p class="fst-italic fw-bold">From: <%= chat.from %>
                </p>
                <p class="fst-italic fw-bold">Message: <%= chat.msg %>
                </p>
                <p class="fst-italic fw-bold">Recived by: <%= chat.to %>
                </p>
                <hr>
                <p style="margin-bottom:0px">
                    <%= chat.created_at.toString().split(" ")[4] %></p>
                    <p><%= chat.created_at.toString().split(" ").slice(0,4).join(" ") %></p>
                    <div class=" d-flex justify-content-evenly">
                        <form class="form" method="GET" action="/chats/<%= chat._id %>/edit">
                            <button class="btn btn-warning">Edit</button>
                        </form>
                        <form class="form" method="POST" action="/chats/<%= chat._id %>?_method=DELETE">
                            <button class="btn btn-danger">Delete</button>
                        </form>
            </div>
    </div>
    <% } %>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
            crossorigin="anonymous"></script>
</body>

</html>